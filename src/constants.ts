export const storeKeys = {
  accessToken: 'accessToken',
  accountId: 'accountId',
  userId: 'userId',
  map: 'map',
  switching: 'switchingEnabled',
};

export const constants = {
  TRACKING_INTERVAL_MS: 60 * 1000,
  CHECK_TRACKING_STATUS_INTERVAL_MS: 5 * 60 * 1000,
};

// export default {
//   keys,
//   constants,
// };